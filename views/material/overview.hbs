{{#extend "lib/loggedin"}}
    {{#content "page"}}
        <section class="section-courses">
            {{#if worksheets}}
                <ul>
                {{#each worksheets}}
                    <li>{{this.title}} (<a href="edit/{{this._id}}">Bearbeiten</a>)</li>
                {{/each}}
                </ul>
            {{else}}
                <div class="col-xs-12">
                    <p class="text-muted text-center">
                        <span>Keine Arbeitsblätter.</span>
                    </p>
                </div>
            {{/if}}

            <div class="col-xs-12">
                <div class="row padding-top">
                    <a href="/material/add" class="btn btn-add btn-primary">Arbeitsblatt erstellen</a>
                </div>
            </div>
        </section>


    {{/content}}
{{/extend}}
