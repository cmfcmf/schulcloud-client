{{#extend "lib/loggedin"}}

    {{#content "styles" mode="append"}}
        <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons" />
        <link rel="stylesheet" href="/vendor/edtrio/viewer.css">
        <link rel="stylesheet" href="/vendor/edtrio/material-components-web.min.css">
    {{/content}}


    {{#content "scripts" mode="append"}}
        <script defer src="/vendor/edtrio/viewer.js"></script>
    {{/content}}


    {{#content "page"}}
        <section class="section-course">
            <div class="col-xs-12">
                <div class="row padding-top">
                    <a href="/material/edit/{{worksheet_id}}" class="btn btn-add btn-primary">Bearbeiten</a>&nbsp;
                    <a href="/material/" class="btn btn-secondary">Zur Übersicht</a>
                </div>
            </div>
            <div id="root"
                 data-api="{{worksheet_api}}"
                 data-worksheet-id="{{worksheet_id}}"
                 data-lti-base-url="{{lti_base_url}}"
                 data-lrs-api="{{lrs_api}}"
                 data-user-id="{{user_id}}">
            </div>

            <div id="modal"></div>

        </section>
    {{/content}}
{{/extend}}
